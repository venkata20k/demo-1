https://git-scm.com/downloads ---> link to download git-bash

create a folder in desktop as git and check git-bash is installed 

check the working dir $pwd

https://github.com/ --> create acount

Create a new repository and named it as demo

copy and paste as it is in git-bash

echo "# demo" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/bschethan99/demo.git
git push -u origin master

refresh the git hub page

yum install git


create dir and open git-bash inside

git --version
git help --> to check the commands use list
git config --global -l --> to check the global config setting
git config --global user.name "chethan"
git config --global user.email "bschethan99@gmail.com"
git config --global -l

git clone https://github.com/bschethan99/demo.git


cd demo/  --> go inside the repo
ls -ltra --> check all the file for git-hub data
git status --> check the working branch and status before
create a file in local  -> use any method for creating a files
git status --> check back the status, you see pending file to git add
git add file.txt --> add the file for stage
git commit -m "added file.txt" --> commit
git status --> it should be ready state to push
git push origin master --> push to git-hub and check the file is present

create a multi file on same path try git --all
git add --all --> this will add all the files
git status
git commit -m "added all files" 
git push origin master

now create a file in git-hub 
git pull origin master --> to pull the data to local without cloneing 


git branch --> to check the brances list
git status --> * shows the active branch
git branch <name> --> creats a branch
git checkout <Bname> --> swith to the branch
git checkout -b <Bname> ---> creats the new branch and switch as well

git push origin <Bname> --> push to the master

git branch -d <Dname> --> to delete the branch 
git branch -D <Dname> --> force delete the branch

 


















